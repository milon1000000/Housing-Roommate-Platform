model AuditLog {
  id        String   @id @default(uuid())
  userId    String
  action    String   // e.g. "PROPERTY_DELETED", "ROLE_UPDATED"
  details   String?
  ipAddress String?
  createdAt DateTime @default(now())

  @@map("audit_logs")
}