enum Role {
  ADMIN
  LANDLORD
  TENANT
}

enum UserStatus {
  ACTIVE
  BLOCKED
  DELETED
}

enum LandlordVerificationStatus {
  PENDING
  APPROVED
  REJECTED
}

enum AuthProvider {
  GOOGLE
  CREDENTIAL
}